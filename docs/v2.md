# Raphael Hub — evolução V2 (roadmap e decisões)

Este arquivo é o **âncora de produto** para funcionalidades que não fazem parte do MVP imediato, mas cuja **estrutura de dados já pode ser preparada** sem quebrar o comportamento atual.

**Regra:** o que é **canônico para implementação** (tabelas, colunas, índices) continua descrito no [SPEC.md](../SPEC.md). Aqui ficam **intenção**, **priorização**, **fluxos futuros** e **como evoluir sem impactar a V1**.

---

## Por que este arquivo existe

- **PRD.md** — visão de produto e MVP; não deve virar um catálogo de schema.
- **SPEC.md** — contrato técnico estável (o que existe no banco e no código **agora**).
- **LLM.md** — convenções de código; apenas aponta para este doc quando necessário.

Quando surgir uma ideia durante o desenvolvimento da base:

1. Se altera schema ou contrato → atualize **SPEC.md** (e as migrations).
2. Se é intenção / ordem de entrega / papéis de usuário → acrescente **aqui**.

---

## V2 — Monitoramento completo de grupos

### Objetivo

Para fontes `tipo = group`, persistir **todo** tráfego relevante: remetente (JID/número), tipo de mensagem, citações, menções, mídias no MinIO com prefixo por fonte, e metadados brutos suficientes para reconstruir contexto depois.

### Tipos de mensagem (extensível)

Os valores são **strings** no banco (não ENUM SQL), para aceitar novos tipos sem migration:

`sistema sugerido (inicial):` texto, áudio, imagem, vídeo, documento, figurinha, localização, contato, enquete, reação, url, desconhecido, outro.

Novos tipos podem surgir da Evolution/API; gravar como `desconhecido` ou `outro` com `metadata` completo até mapearmos.

### Pipeline de enriquecimento (futuro)


| Ativo  | Etapa                               | Fallback                        |
| ------ | ----------------------------------- | ------------------------------- |
| Áudio  | Transcrição Whisper (local)         | NeuronAI / provedor configurado |
| Imagem | (sem OCR local confiável no início) | LLM visão via NeuronAI          |
| Texto  | Classificação / categorias          | **Base Hub:** `AiRouterService` + `POST /iara` prontos; persistência em `message_logs` / jobs de rota ainda a ligar |

**Estado atual (2026):** stack de inferência (`neuron-core/neuron-ai`, Ollama→Groq→Anthropic→OpenAI, gateway `/iara`, logs `ai.completion`) está descrita no [SPEC.md](../SPEC.md) e no [LLM.md](../LLM.md). O backlog abaixo foca **produto** (grupos, UI, persistência), não repetir essa infra.


Campos dedicados na tabela de mensagens (`transcription_`*, `vision_*`, status de processamento) permitem retries idempotentes e auditoria **sem** misturar tudo em JSON opaco.

### Armazenamento S3 / MinIO

- Bucket compartilhado (`pessoal`), **prefixo por fonte monitorada** (`monitored_sources.media_storage_prefix`).
- Anexos em tabela própria (`message_attachments`) com `storage_path` = chave no bucket.
- Evolução futura: política de lifecycle por prefixo (não depende do MVP).

---

## V2 — Dashboard e permissões por grupo

### Personas

- **Dono global (Raphael):** acesso a pessoal + todos os grupos monitorados + configurações sensíveis.
- **Admin de grupo (futuro):** acesso ao dashboard **somente** dados ligados a uma ou mais fontes `group` específicas (ex.: moderar leitura de histórico/mídia daquele grupo).

### Modelo de dados (preparação)

- `users.global_role`: `super_admin` | `member` — define se o usuário pode operar em escopo global (super_admin) ou apenas no que for explicitamente concedido (`member`).
- `monitored_source_user`: vínculo N:N entre usuário e fonte monitorada, com papel no contexto daquela fonte (ex.: `group_admin`, `viewer`).

A **V1** pode ignorar totalmente o pivot: só você existirá como `super_admin` via seeder/manual. A existência da tabela **não quebra** o app.

### Autorização no código (quando implementar)

- Gates/policies checando `global_role === super_admin` OU existência de registro em `monitored_source_user` com papel adequado para a rota recurso.

---

## Como evoluir sem quebrar a V1

1. **Novas colunas opcionais** com default / nullable — ok.
2. **Novas tabelas** referenciando as existentes — ok; código antigo não as usa.
3. **Evitar** renomear colunas em uso; se inevitável, migration de renomeação + janela de deploy.
4. **Feature flags** (.env) para ativar Whisper, LLM visão, ou UI de “admin de grupo” quando estiver pronto.

---

## Backlog sugerido (ordem não rígida)

1. Webhook estável + persistência completa grupo + anexos no MinIO.
2. Jobs de transcrição (fila dedicada) com retry e marcação de status.
3. **Ligar** classificação/categorias ao pipeline existente (`NeuronAIService::complete` / `AiTask`) nos jobs WhatsApp + eventual coluna “responsável” — schema no SPEC antes se precisar de novos campos.
4. UI admin por grupo + convites (se necessário).

---

## Changelog deste documento


| Data       | Nota                                                                                                                                                                |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-04-17 | Criação inicial alinhada à primeira leva de migrations estendidas.                                                                                                  |
| 2026-04-17 | Produto: grupo WhatsApp “só você” como workaround para mídia quando Evolution não webhooka o chat 1:1; mesmo JID tratado como rota pessoal no hub (`SPEC` / `LLM`). |
| 2026-04-22 | Documentação de infra: MinIO e Evolution próprios na VPS; Ollama no host com acesso restrito à bridge Docker — detalhes em `README.md` / `SPEC.md` / `PRD.md`.      |
| 2026-04-23 | IA: implementação `AiRouterService`, `OllamaService`, `NeuronAIService`, pacote `neuron-core/neuron-ai`, gateway `POST /iara`, `config/ai.php`; SPEC/LLM/README atualizados. Roadmap: próximo passo é **uso nos jobs** + classificação persistente, não mais “subir stack de providers”. |
| 2026-04-23 | Threads Classificados (Fase 0): serviço Playwright autenticado implementado (`playwright/`), endpoints `/threads/auth/login`, `/threads/scrape-url`, `/threads/scrape-keyword`, modo keyword `posts-only` e dedupe opcional no scraper (`known_post_ids`, `only_new`, `known_streak_stop`) documentados no `SPEC.md`. |
| 2026-04-23 | Threads Classificados (Fase 1): fundação Laravel concluída com schema `threads_*`, models (`ThreadsSource`, `ThreadsPost`, `ThreadsComment`, `ThreadsCommentVote`, `ThreadsCategory`) e seed inicial de categorias. |
| 2026-04-23 | Threads Classificados (Fase 2): contrato mockável `ThreadsScraperClientInterface`, cliente HTTP `ThreadsPlaywrightService`, fake `FakeThreadsScraperClient`, bind no container e teste de integração em `tests/Feature/Threads/ThreadsScraperClientTest.php`. Próxima etapa: Fase 3 (jobs + classificação IA). |
| 2026-04-23 | Threads Classificados (Fase 3.1): pipeline base no Laravel com `ScrapeThreadsUrlJob` e `ScrapeThreadsKeywordJob` (fila `scraping`) + `ThreadsScrapeIngestionService` para upsert idempotente de `threads_posts`/`threads_comments` por `external_id`, coberto por `tests/Feature/Threads/ThreadsScrapeIngestionJobsTest.php`. |
| 2026-04-23 | Threads Classificados (Fase 3.2): classificação IA de comentários com `ThreadsClassificationService` (`NeuronAIService::complete` com JSON), `ClassifyCommentsJob` na fila `ai`, aplicação de `THREADS_RELEVANCE_THRESHOLD` para status (`ignored`/`pending_review`), supervisor `ai` no Horizon e cobertura em `tests/Feature/Threads/ThreadsClassificationServiceTest.php`. |
| 2026-04-23 | Threads Classificados (Fase 4.1): instalação de `livewire/livewire` v4, integração base nos layouts Blade (`@livewireStyles`/`@livewireScripts`) e smoke test de disponibilidade do pacote em `tests/Feature/Livewire/LivewireInstallationTest.php`. |
| 2026-04-23 | Threads Classificados (Fase 4.2): estrutura inicial do dashboard autenticado em `/hub/threads` com `App\Livewire\Threads\HubPage`, abas `Sources/Review/Published`, tabela de `threads_sources`, link na navegação e cobertura em `tests/Feature/Threads/ThreadsHubPageTest.php`. |
| 2026-04-23 | Threads Classificados (Fase 5.1): sources management inicial no `HubPage` com criação (keyword/url), toggle ativo/inativo e ação `scrape agora` disparando `ScrapeThreadsKeywordJob`/`ScrapeThreadsUrlJob`; testes Livewire cobrindo ações no `ThreadsHubPageTest`. |
| 2026-04-23 | Threads Classificados (Hardening IA): `ClassifyCommentsJob` refatorado para 1 comentário por execução (reprocessamento manual por id), novo `DispatchPendingThreadsClassificationJob` para varrer pendentes com espaçamento (`THREADS_AI_DISPATCH_SPACING_SECONDS`) e menor risco de travamento/cota. |
| 2026-04-23 | Threads Classificados (Fase 5.2 inicial): aba `Review` com filtro de status, destaque de `ignored`, ações manuais de curadoria (reclassificar, mover para review, ignorar, publicar/despublicar) e gatilho manual de classificação pendente no Hub. |
| 2026-04-23 | Threads Classificados (Fase 5.2 consolidada): curadoria de `Review` com seleção múltipla + ações em lote (`mover para review`, `ignorar`, `publicar`, `despublicar`, `reclassificar`), filtros por status/categoria/source/sem resumo IA e ordenação por relevância/mais novo/score, com cobertura em `ThreadsHubPageTest`. |
| 2026-04-23 | Threads Classificados (Fase 5.3): aba `Published` no Hub com listagem só de `is_public`, filtros `pub_category`/`pub_source`, ordenação `pub_sort`, edição rápida de resumo/categoria/destaque, métricas de votos/score e despublicar; documentado em `SPEC.md`/`README.md` e testado em `ThreadsHubPageTest`. |
| 2026-04-23 | Threads Classificados (Hub UX): contadores de pendentes de classificação (`ai_summary` nulo), cadência da fila `ai` em `config/services.php`, mensagem de flash ao disparar batch, e checkbox “selecionar todos” na Review; testes no `ThreadsHubPageTest`. |
| 2026-04-23 | Threads Classificados (página pública inicial): rota `GET /oportunidades` com listagem SSR só de comentários `is_public`, filtros e ordenação básicos, layout `layouts.public`, testes `ThreadsOpportunitiesPageTest`. |
| 2026-04-23 | Threads Classificados (votação pública): `POST /oportunidades/votos/{comment}` com fingerprint diário, `threads_comment_votes`, `RecalculateCommentScoreJob`, variável `THREADS_VOTE_FINGERPRINT_SALT`; testes `ThreadsCommentVoteTest`. |
| 2026-04-24 | Embasa/Coelba (Bloco 1): Playwright ganhou `POST /embasa/scrape` e `POST /coelba/scrape`, health com estado de sessão por provider e persistência de `storageState` dedicado (`EMBASA_SESSION_PATH`, `COELBA_SESSION_PATH`) com relogin automático quando sessão expira. |
| 2026-04-24 | Coelba (hardening fluxo real): scraper refeito para navegação determinística sem depender de sessão reaproveitada, executando login completo e passos obrigatórios (`selecionar-estado` -> `meus-imoveis` -> `home` -> `Faturas e 2ª via` -> `consultar-debitos`) antes da extração/download. |
| 2026-04-24 | Coelba (home-first): diante da instabilidade em `consultar-debitos`, o fluxo passou a extrair dados do card `Última Fatura` na home e executar download da 2ª via pelo menu `Mais opções` + modal de motivo (`Não Recebi`), com captura opcional de código PIX por modal. |


